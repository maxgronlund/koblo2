$('#user_pictureQueue').hide();
$('.profile_picture').replaceWith("<%= escape_javascript(image_tag(@picture.picture.url(:upload))) %>");
$('#user_password').append("<input type='hidden' name='user[picture_id]' value='<%= @picture.id %>'/>')");

