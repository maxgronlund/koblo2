- content_for :sub_content do
  %h1 Stuff I bought
  %ul
    - @purchase_items.each do |purchase_item|
      - song = purchase_item.song
      %li= song.user.name
      %li= song.title
      %li= link_to 'download', purchased_song_download_path(current_user, purchase_item)
